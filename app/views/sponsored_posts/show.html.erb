<h1><%= @sponsoredpost.title %></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @sponsoredpost.body %></p>
   </div>
   <div class="col-md-8">
     <p>Price: $<%= @sponsoredpost.price %>.00</p>
   </div>
   <div class="col-md-4">
     <%= link_to "Edit", edit_topic_sponsored_post_path(@sponsoredpost.topic, @sponsoredpost), class: 'btn btn-success' %>
   </div>
 </div>
